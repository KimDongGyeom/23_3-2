import numpy as np

mid_scores = np.array([10, 20, 30])
final_scores = np.array([70, 80, 90])

a = mid_scores + final_scores
b = mid_scores - final_scores

print('합계:', a)
print('평균:',a/2)
print('마이너스:',b)

